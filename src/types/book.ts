// src/types/book.ts

export interface BookInfo {
  title: string;
  authors: string;
  publisher: string;
  isbn: string;
  coverUrl: string;
  description: string;
  pageCount: number;
  language: string;
  source: string;
}
